# Admin Flow

Admin users can open the admin panel, review user profiles, create categories and moderate listing status. The admin role is stored in `user_roles` and protected by Supabase RLS policies.
